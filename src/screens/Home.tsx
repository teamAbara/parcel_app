import React, { useEffect, useState } from "react";
import { Text, ScrollView } from "react-native";

const HomeScreen = ({ navigation }: any) => {
  return (
    <ScrollView bounces={false}>
      <Text style={{ color: "white" }}>dafffad</Text>
    </ScrollView>
  );
};

export default HomeScreen;
